export enum ContainerSizes {
  S = "s",
  M = "m",
  L = "l",
  XML = "xml",
  XL = "xl",
  XXL = "xxl",
  CUSTOM = "CUSTOM",
}

export enum ImageSizes {
  S = "s",
  M = "m",
  L = "l",
  XL = "xl",
  FULL_SIZE = "FULL_SIZE",
  XXL = "xxl",
}

// for the distinction between user and project rights
export enum RoleMmbership {
  USER = "USER",
  PROJECT = "PROJECT",
  NONE_RESOURCE = "NONE_RESOURCE",
}
